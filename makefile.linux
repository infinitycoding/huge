SRCS = $(shell find -name '*.cpp')
OBJS = $(addsuffix .o,$(basename $(SRCS)))

INCLUDE = -I/usr/include/ -Iinclude
HUGE = huge.a

CC = g++
CFLAGS = -Wall $(INCLUDE)

all: $(OBJS) lib

%.o : %.cpp
	$(CC) $(CFLAGS) -c -o $@ $<

lib: $(OBJS)
	ar -rcs $(HUGE) $^

clean:
	rm $(OBJS)
	rm $(HUGE)

